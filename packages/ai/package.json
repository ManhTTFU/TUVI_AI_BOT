{
  "name": "@tuvi/ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "//": "Gói 'openai' được dùng làm HTTP client cho Deepseek (OpenAI-compatible API). Xem src/client.ts để rõ — baseURL được trỏ sang https://api.deepseek.com, không gọi về api.openai.com.",
  "dependencies": {
    "@tuvi/astrology": "workspace:*",
    "@tuvi/core": "workspace:*",
    "openai": "^4.77.0",
    "p-limit": "^7.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
